# Dev: Vite proxy to Flask (http://localhost:5000)
VITE_API_BASE_URL=/api

# Production (Vercel): use /api — vercel.json proxies /api/* to Render (avoids CORS).
# Do NOT set the full Render URL unless you also configure CORS_ALLOWED_ORIGINS on the backend.
# VITE_API_BASE_URL=/api
